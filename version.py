#!/usr/bin/env python3
"""Project version.

Update this manually. A release is a `vX.Y.Z` Git tag plus a GitHub release;
`release-docker.yml` turns it into semantic version tags in the container registry.
"""
__version__ = "0.1.1"
